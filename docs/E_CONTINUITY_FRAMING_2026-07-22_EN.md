# E-Continuity — the parent framework (roof of the ecosystem)

DATE: 2026-07-22
BASIS: AUTHOR_DECISION AD-13 — all projects (Notarius, MSL/MIP,
Foundation Layer, SSP, etc.) are fruits of one parent:
**E-Continuity / the Framework of Governed Recoverability.**
STATUS: FRAMING_DOCUMENT / roof
UPDATED 2026-07-22 (AD-14): the canonical definition of the parent has been
RECEIVED and read (docs/e_continuity/, analysis —
E_CONTINUITY_ANALYSIS_2026-07-22_EN.md). The inference of "governed
recoverability" is CONFIRMED by the source verbatim. The lineage is
corrected: Notarius is a grandchild via MSL/MIP, not a direct child.

---

## What the author recorded

Ruslan Malyavskiy, 2026-07-22:
> "All projects are fruits of E-Continuity / the Framework of Governed
> Recoverability, so everything can be recorded within the project."

Consequence for this repository: Notarius and the imported
Foundation Layer (MSL/MIP) are not foreign projects in one box,
but **siblings under a common parent**. Keeping them together is not a violation
of `ECOSYSTEM ≠ MONOLITH`, but its correct application: one ecosystem,
different branches, with the boundaries between branches signed.

## Kinship map (CORRECTED per the E-Continuity source, AD-14)

```
E-Continuity / Governed Recoverability        ← PROTO-FATHER
  thesis: OBJECT_PRESERVED ≠ MISSION_RECOVERABLE
        │
        ├── MSL/MIP Sign Alphabet      → sign identity
        │      SIGN_EXISTS ≠ MEANING_RECOVERABLE
        │      ├── Notarius   → element provenance (SIGNED ≠ NATIVE)
        │      └── SSP        → meaning provenance
        │
        ├── AI Conveyor Workbench      → REVIEW_COMPLETED ≠ VALIDATED
        └── Foundation Layer (FO)      → HEAD ≠ SOURCE
```
Notarius is a GRANDCHILD of E-Continuity via MSL/MIP (not a direct child, as
it was in the first edition). Source of the kinship: E-Continuity itself declares
itself the proto-father (MIGRATION_E_CONTINUITY_v0_2, pp. 263–271).

## The unifying theme — CONFIRMED BY THE SOURCE (was: an inference)

The canonical definition has been received (AD-14): the framework is literally
`governed recoverability through time`. The inference is confirmed. The through
line of "governed recoverability" is the EXPLICIT thesis of the parent, not
my guess:

- Notarius: recover the **provenance** of an element (where it came from,
  where the break is) — recoverability of provenance.
- Foundation Layer FO-033: recover the **governability** of a system through
  selective pruning (`ROLLBACK ≠ RECOVERY`).
- FO-043 PATCH_A: `RESIDUAL_MEANING = RECOVERY_POTENTIAL` — recover
  **meaning** through residual traces (STATUS_ROLLBACK).
- The migration package: recover the **working context** without losing the core.
- AD-10 Notarius: the chain localizes where the lie entered — recover
  **the addressability of responsibility**.

The common pattern (a candidate, not a proven law — in the spirit of FO-040
`DO_NOT_SPIN_OFF_YET`):
```
BREAK / LOSS / SUBSTITUTION
→ A VISIBLE, LOCALIZED, EXPLAINABLE TRACE
→ GOVERNED RECOVERY (not complete, not automatic)
```
Notarius is the implementation of this pattern at the data-element level.

## The cross-cutting pattern of three generations (AD-17)

From the MSL/MIP genesis (docs/MSL_MIP_GENESIS_PROVENANCE) it is visible that the whole
vertical carries one thought: **a meaningful unit is saved by its boundary.**
Each generation — the same boundary against its own loss:

```
GRANDFATHER  E-Continuity : the boundary survives loss of CONTEXT in TIME
                            (mission across thousands of years; Viking)
FATHER       MSL/MIP       : the boundary survives loss of LANGUAGE
                            (meaning across millennia; the bracket (word))
GRANDCHILD   Notarius      : the boundary survives loss of TRUST
                            (provenance through others' hands; chunk + boundary)
```

The direct hereditary line by mechanism:
the bracket `(word)` → `block_with_witness` → length-witness. In all three
an extra/missing sign is visible from the **breach of the boundary** — one canary,
three substrates. The bracket on stone and the codepoint counter are direct kin.

CANDIDATE FORMULA (not a law — in the spirit of FO-040 / RECURRENCE ≠ VALIDITY):
```
MEANINGFUL_UNIT_SURVIVES_THROUGH_ITS_BOUNDARY
BOUNDARY_BREAK = FIRST_DETECTABLE_SIGNAL
(loss: context / language / trust — the mechanism is one)
```
Status: RECURRING_PATTERN_CANDIDATE / WORTH_STUDYING / NOT_UNIVERSAL_LAW.

## What this changes in practice

1. Foundation Layer stays in the repository (option 1) — now with an
   architectural basis, not "tolerated." It is a neighboring branch of
   one ecosystem, sitting in its own folder `docs/foundation_layer/`.
2. The boundaries between branches remain signed
   (FOUNDATION_LAYER_ANALYSIS draws the line "what transfers / what doesn't")
   — `BRANCH_INDEPENDENCE = GOVERNABILITY` from ECOSYSTEM_MAP.
3. The PR (if made) merges the branch into main of this repository — it does not
   touch the E-Continuity roof and does not affect other branches of the ecosystem.

## Open item — CLOSED (AD-14)

The canonical definition of E-Continuity has been received and recorded:
the text core in docs/e_continuity/, the full breakdown in
E_CONTINUITY_ANALYSIS_2026-07-22_EN.md. The heavy PDF/DOCX editions
(master archive v2.3) are left outside the repository as a fallback zone
(per the framework's own CURRENT / LEGACY distinction).

STATUS:
FRAMING_DOCUMENT
PARENT_DEFINITION_RECEIVED (AD-14)
UNIFYING_THEME_CONFIRMED_BY_SOURCE
LINEAGE_CORRECTED (Notarius = grandchild via MSL/MIP)
